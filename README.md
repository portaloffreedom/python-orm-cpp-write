# python-orm-cpp-write
A test to use a python framework for database management, but the data is managed also in c++

### Requirements

This project will need a postgres instance already already running
